#ifndef ML_DEBUG_H
#define ML_DEBUG_H

class  MNode;
class  MlEnv;

#if 0
MNode*  ml_sleep (bool fev, MNode* cell, MlEnv* mlenv);
#endif
MNode*  ml_stderr (bool fev, MNode* cell, MlEnv* mlenv);

#endif /* ML_DEBUG_H */
